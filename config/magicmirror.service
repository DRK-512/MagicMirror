[Unit]
Description=MagicMirror
After=network.target
StartLimitIntervalSec=0

[Service]
Type=simple
User=mm
ExecStart=/bin/sh -c "cd /home/mm/MagicMirror && DISPLAY=:0 npm run start:x11"

[Install]
WantedBy=multi-user.target
